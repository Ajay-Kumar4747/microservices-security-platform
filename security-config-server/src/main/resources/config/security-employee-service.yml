server:
  port: 8081

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

  data:
    mongodb:
      uri: mongodb://localhost:27017/employee